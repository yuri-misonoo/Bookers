<h1>Book</h1>

<% @books.each do |book| %>
  <div class="index-content">
    <h4>Title</h4>
      <span><%= book.title %></span>
    
    <h4>Body</h4>
      <span><%= book.body %></span>
  </div>
<% end %>
  
